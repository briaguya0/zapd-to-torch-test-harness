__pycache__/
*.pyc

# Local Shipwright AppImage / CI build used to produce the reference O2R
shipwright-bin/

# Gate A/A' static-lib build trees
torchlib-driver/build*/

# Torch build trees (baseline, gate B oot-only, gate C release, ...)
torch/build*/
